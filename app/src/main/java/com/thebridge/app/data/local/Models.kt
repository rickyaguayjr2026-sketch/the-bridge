package com.thebridge.app.data.local

enum class UserMode {
    FOLLOWER,
    CAREGIVER,
    ABIDE,
}

enum class EventSource {
    WORD,
    CONVERSATION,
    SYSTEM,
}

enum class ConversationActor {
    USER,
    ASSISTANT,
}
